import React from "react"

import sl from './MonthStatistic.module.scss'

interface Props {

}

export const MonthStatistic = (props: Props) => {
    return (
        <div className={sl.home}>
            MonthStatistic
        </div>
    )
}